package aula;

import static org.junit.Assert.*;

import org.junit.Test;

public class ExercicioSeisESete {

	@Test
	public void test() {
		/*
		 *  Por que a frase a seguir está errada?
				“Uma classe parametrizada pode ser usada para
				 otimizar qualquer conjunto de teste”
			R: Esta frase está errada pois uma classe parâmetrizada 
			não pode ser usada em todos os casos, isso porque os parâmetros que 
			serão testados podem não corresponder ao que é necessário para validar certos testes.
			
			 O que é uma Suite de testes?
			 R: É um conjunto de testes que facilita o gerênciamento dos muitos testes possíveis em um projeto,
			 isso porque com suites de testes, eles poderão ser segmentados por partes do projeto e com isso
			 aumentar a organização dos projetos.
		 */
	}
}
